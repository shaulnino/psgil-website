harden: centralized permissions, appeal-aware penalties, auth audit fixes

- Add can/hasRole/hasAnyRole helpers + StewardPermission matrix in auth.ts
- Replace scattered role.includes() checks across steward pages with named can() calls
- Fix computeDriverPoints to be appeal-aware (renamed computeEffectiveDriverPoints),
  aligning threshold penalty generation with aggregateDriverPenalties display logic
- Consolidate upsertAppealVerdictAction, publishAppealVerdictAction,
  updateAppealStatusAction to use requireRole() instead of inline checks
- Fix attachment API: add authentication guard (401 for unauthenticated requests)
- Fix attachment API: Cache-Control changed from public/immutable to private/no-store
  to prevent CDN caching of authenticated content on Netlify
- Add runtime warning when STEWARD_SESSION_SECRET is missing or uses dev fallback
  in production (surfaces immediately in Netlify function logs)
- Cases table: move case number into its own # column separate from case title
